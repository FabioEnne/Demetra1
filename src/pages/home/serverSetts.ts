import { Component } from '@angular/core';
import { NavController } from 'ionic-angular';

@Component({
    selector: 'page-serverSetts',
    templateUrl: 'serverSetts.html'
})
export class ServerSetts {
    constructor(public navCtrl: NavController) {}
}